import 'package:flutter/material.dart';

class SosItem extends StatelessWidget {
  const SosItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
