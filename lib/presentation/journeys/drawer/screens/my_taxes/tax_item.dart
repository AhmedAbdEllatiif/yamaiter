import 'package:flutter/material.dart';

class TaxItem extends StatelessWidget {
  const TaxItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
